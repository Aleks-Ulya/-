1.Вариант 5. Напишите рекурсивную функцию для проверки, является ли строка палиндромом.
2.Описание алгоритма с синтаксисом:
Как работает:
 1.normalize() проходит по строке, удаляет пробелы и переводит в нижний регистр.
 2.isPalindrome() вызывает рекурсивную проверку с полными границами.
 3.isPalindromeRecursive():

        При left ≥ right возвращает true.

        При несовпадении символов — false.

        Иначе рекурсивно проверяет подстроку с сдвинутыми границами.

 4.main() тестирует функцию на наборе строк.
3.Временная сложность алгоритма:
O(N)
4.Описание временной сложности (почему?):
Поскольку каждая рекурсия имеет фиксированную стоимость O(1), а общее число рекурсий линейно зависит от длины строки, итоговая временная сложность получается линейной относительно длины строки.
Контрольный вопрос 8.Сколько существует перестановок множества из n элементов? 
Количество перестановок множества из n различных элементов равно n!. То есть, это произведение всех целых чисел от 1 до n.
